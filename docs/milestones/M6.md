# M6 - Personal history and reconciliation

## Goal
Persist saved operations and realized outcomes; distinguish realized and unrealized profit; support local clearing.

## Dependencies
See the roadmap in `docs/specs/project-spec.md` and ticket dependency fields.

## Completion rule
All tickets in this milestone meet acceptance criteria, tests pass, and any VERIFY items are either resolved or explicitly carried forward.
