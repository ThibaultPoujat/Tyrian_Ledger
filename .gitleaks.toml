title = "Tyrian Ledger Gitleaks configuration"

[allowlist]
  description = "The environment-variable identifier is not a credential value."
  regexTarget = "line"
  regexes = [
    '''^\s*public const string Gw2ApiCredentialEnvironmentVariable = "TYRIAN_LEDGER_GW2_API_KEY";$''',
  ]
